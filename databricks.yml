# This is a Databricks asset bundle definition for coffee_weather.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: coffee_weather

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  service_principal_id:
    description: Service principal for production

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com
    variables:
      warehouse_id: 
        lookup:
          warehouse: Serverless Shared Endpoint
      catalog: dnb_wave_5
      schema: sandbox        

  ## Optionally, there could be a 'staging' target here.
  ## (See Databricks docs on CI/CD at https://docs.databricks.com/dev-tools/bundles/ci-cd.html.)
  #
  # staging:
  #   workspace:
  #     host: https://e2-demo-field-eng.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    # We use 'mode: production' to indicate this is a production deployment.
    # Doing so enables strict verification of the settings below.
    mode: production
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com
      # If this path results in an error, please make sure you have a recent version of the CLI installed.
    variables:
      warehouse_id: 
        lookup:
          warehouse: dbdemos-shared-endpoint
      catalog: dnb_wave_5
      schema: prod
      service_principal_id: c6895172-0f52-400a-99dc-390e867f5f8b
        # this will take forever in e2-demo-field-eng
        # lookup:
        #   service_principal: eb-dnb-wave-5-sp 
    run_as:
      service_principal_name: ${var.service_principal_id}
    resources:
      jobs:
        coffee_weather:
          name: "[prod ${workspace.current_user.userName}] Coffee Weather"
